/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['m.media-amazon.com', 'http2.mlstatic.com', 'https://www.antryx.com', 'www.antryx.com'], // Agrega el dominio aquí
    },
  };
  
  export default nextConfig;
  